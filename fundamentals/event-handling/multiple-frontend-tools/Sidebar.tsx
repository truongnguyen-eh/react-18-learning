import React from 'react';

const Sidebar = () => (
  <div className="sidebar" style={{ border: '2px solid #222', borderRadius: 20, padding: 24, margin: 0, minWidth: 200, fontWeight: 'bold', fontSize: 28, height: '100%' }}>
    Sidebar
  </div>
);

export default Sidebar; 