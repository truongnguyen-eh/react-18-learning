import React from 'react';

const RenderList = () => (
  <div className="leave-row" style={{ border: '2px solid #222', borderRadius: 16, padding: 18, marginBottom: 18, fontSize: 22 }}>
    Render list
  </div>
);

export default RenderList; 